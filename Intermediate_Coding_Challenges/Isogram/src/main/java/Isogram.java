
public class Isogram {
    /**
     * An Isogram is a word where no letters are repeated.
     * For instance, "cat", "isogram", "uncopyrightable" are isograms. "egg", "java", "programming" are not.
     * You could use either a Set or a Map to solve this problem. Note that it's also possible to solve this with
     * nested for loops.... but it's not as easy or efficient.
     * @param str a String.
     * @return true if str is an isogram, false otherwise.
     */
    public boolean isIsogram(String str){
        return false;
    }
}
